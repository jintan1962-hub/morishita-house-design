# CLAUDE.md

このプロジェクトで作業するAIエージェント（Claude Code / Gemini CLI / その他）が必ず守るルール。
詳細規定：`~/Desktop/システム開発マスタールール/v2/SECURITY_RULES.md` / `OPS_RULES.md` / `README.md`

---

## プロジェクト固有欄
```
プロジェクト名：中古住宅専門店 モリシタハウス サイト（morishita-used-housing-site）
　　　　　　　　※ used-housing-site（RENOEL／大井建設工業）とは**別プロジェクト・別デプロイ先**。
　　　　　　　　　仕組みだけを引き継いでおり、リポジトリ・DB・ドメインは共有しない
解決する業務：物件掲載・会員限定物件の出し分け・会員管理・問い合わせ受付（詳細 SYSTEM_SPEC.md）
利用者と権限：未ログイン=公開物件のみ／会員(USER)／管理者(ADMIN)。role の付与はDB直接操作のみ
扱う個人情報：有（氏名／メール／電話／郵便番号／住所／問い合わせ本文／閲覧履歴）→ 触る変更は全てL3
本番環境：【未構築】Vercel／Supabase(PostgreSQL)／メールは Resend(REST)。
　　　　　　モリシタハウス用の Supabase・Vercel は未作成（2026-08-25 時点）。
　　　　　　構築手順と役割分担は docs/本番環境の作り方.md。Supabaseは**新規プロジェクト**で作る（大野の決定）。
　　　　　　RENOEL の本番接続情報は .env.renoel-production.bak / .env.renoel-migrate.bak へ退避済み。
　　　　　　**この2ファイルを .env に戻さないこと**（他社の本番DBに繋がる）
GAS WebApp：該当なし（Next.js / Vercel 構成）
機械ゲート：`pnpm gate`。秘密情報スキャンは .githooks/pre-commit（git config core.hooksPath .githooks）
開発用DB：Docker コンテナ `morishita-dev-db`（postgres:16-alpine／ホスト側ポート **5434**／
　　　　　ボリューム morishita-dev-db-data）。起動 `pnpm db:up` ／ 停止 `pnpm db:down`。
　　　　　RENOEL版の `renoel-dev-db`（ポート5433）とは別コンテナ・別ボリュームでデータを共有しない。
　　　　　接続情報は .env.local にのみ書く（.gitignore 対象）。投入は `pnpm seed:dev`
デプロイ：**AIが実行する**（2026-08-19に大野が変更。D-14を参照。O-04はこのプロジェクトでは適用しない）
　　　　　　`git push` / Vercelへのデプロイ / `prisma migrate deploy` まで。実行前に毎回確認を取る
バックアップ：対象=Supabase全テーブル／頻度・保存先(Supabase外にも1本)・保持期間・復旧手順【要記入】
復旧目標：復旧時間・許容データ損失【要記入】（先に決めてから頻度を決める）／連絡先：一次・二次・初報期限【要記入】
業務ルールの集約先：src/config/ 配下
　　　　company.ts=社名住所電話差出人免許番号／loan.ts=金利年数／security.ts=権限
　　　　property.ts=掲載エリア(兵庫県20市町)・物件種別／himejiAreas.ts=姫路市内の地区と小学校区
　　　　navigation.ts=メニュー／staff.ts=スタッフと代表メッセージ／analytics.ts=計測タグID
変更点マップ：docs/変更点マップ.md
停止スイッチ：Supabase の SystemSetting で key='MAIL_SENDING_ENABLED' を 'false' → 全メール送信が即時停止
　　　　　　　　（デプロイ不要）。予備：環境変数 MAIL_SENDING_DISABLED=true
送信結果の確認：管理画面 /admin/mail-logs（MailLog テーブル）。「失敗」が出ている間は自動返信が届いていない
```

## 固有の注意（過去に事故った箇所）
1. `"use server"` は**直接叩ける公開エンドポイント**。返す／変える関数の冒頭で必ず
   `requireAdmin()` か `requireUser()`（`src/lib/auth.ts`）を呼ぶ。
2. 会員限定物件の秘匿は**サーバー側**で。クライアントで隠すだけではソースから価格が読める。
3. `NEXTAUTH_SECRET` にフォールバック値を置かない（固定値をpublicリポジトリへ公開した前科あり）。
4. 会員削除は論理削除（`deletedAt`）。参照系は必ず `deletedAt: null` で絞る。
5. 本番DBを直接触るスクリプトをリポジトリ直下に置かない（docs/debt.md）。
6. **架空データを画面に置かない。**このプロジェクトは RENOEL 版から複製したため、
   仙台・福島の施工事例、実在しないショールーム、創作のお客様の声が入っていた前科がある。
   実データが無い画面は「掲載準備中」にする（2026-08-25 の作業ログ参照）。
7. **他社の計測タグ・画像・ロゴを引き継がない。**GA4・Meta Pixel などのIDは
   src/config/analytics.ts で環境変数からのみ読む。直書きしない。

## D-01 作業開始時に変更レベルを宣言する

L1（軽微）/ L2（通常）/ L3（重要）のどれかを最初に宣言する。判定基準は README.md 4章。
迷ったら上のレベルを採る。L3は**着手前に人間の承認**が必要。

【判定】応答の冒頭にレベル宣言があるか。

## D-02 L3では実装前に2案以上を出す

案ごとに「利点／欠点／戻し方」を書く。1案しか無い場合は「なぜ他に無いか」を書く。
L1・L2では不要。**軽微な修正に儀式を課さない。**

【判定】L3の作業ログに2案以上の比較が残っているか。

## D-03 推測で埋めない

仕様が不明な箇所は、推測で実装せず**質問する**か、`TODO:未確認` を残して報告する。
「たぶんこうだろう」で書いたコードは、そうと明記する。

【判定】仕様外の挙動が実装されていないか。レビュー時に「これはどこで決めた仕様か」と1点抜き打ちで問う。

## D-04 勝手に消さない・改名しない・仕様を変えない

既存のファイル、関数、DBカラム、設定値の削除・リネームは、L3として承認を得てから行う。
「不要に見えた」は理由にならない。

【判定】diffに削除・リネームがあるとき、承認記録があるか。

## D-05 実装後は必ず機械ゲートを通す

**AIの「テストは全て通りました」という自己申告は証拠として採用しない。**
コマンドを実際に実行し、**出力をそのまま貼る**こと。落ちたら落ちたと報告する。

| 系統 | コマンド |
|---|---|
| Python (uv) | `uv run pytest -q` ／ Lint導入済なら `uv run ruff check .` |
| TypeScript / Next.js | `npm run typecheck && npm run lint && npm run test` |
| Google Apps Script | `node harness.js`（検品ハーネス）＋ 手動で画面確認 |
| 全共通（コミット前） | `git status` と `git diff --cached` を目視、秘密情報スキャン（S-01） |

【判定】報告に実行コマンドと生の出力が含まれているか。「通りました」だけの報告は**不合格**。

## D-06 テストを書く対象

以下は必ずテストを書く。それ以外は任意。
- 金額・日数・件数の計算
- 権限判定
- 外部データの取込（CSV/スプレッドシート/API）のパース
- 削除・一括更新の処理

【判定】上記に触れたコミットに対応するテストが存在するか。

## D-07 エラーを握り潰さない／利用者に内部情報を見せない

例外を握って握りっぱなしにするコード（空のcatch、`except: pass`、エラーを無視した`if`）は禁止。
握る場合は「ログに残す」か「呼び出し元に返す」のどちらかを必ず行う。

**利用者の画面には、スタックトレース・SQL文・内部パス・変数の中身を出さない。**
出すのは「処理できませんでした」＋問い合わせ用のID（日時＋連番など）まで。
詳細はログに残し、ログ側は個人情報を含めない（S-09）。

【判定】`except:\s*pass` / `catch\s*\([^)]*\)\s*{\s*}` が0件。
　　　　わざとエラーを起こし、画面に内部情報が出ないことを1件確認する。

## D-08 設定値をコードに直接書かない（ただし秘密情報は設定ファイルにも書かない）

会社名・住所・メール・URL・料金・しきい値・IDは設定ファイルにまとめる。
**APIキー・パスワード・トークンは設定ファイルにも書かない。**環境変数またはシークレット管理経由（S-01）。

【判定】同じ定数が2箇所以上に直書きされていないか。設定ファイルに鍵らしき文字列が無いか。

## D-09 1つの仕様変更で直す箇所は1箇所にする

同じ処理を2箇所以上に書かない。**書き方が違っても「同じことをしている」なら2箇所目と数える。**

【判定】同一の処理・判断が2箇所以上にあれば不合格。
　　　　（例外：早すぎる共通化を避けるため、**2箇所目を書いた時点で共通化する**。3箇所待たない）

## D-10 サイズ上限

超えたら分割を検討する。超えること自体は禁止ではないが、**超えた理由を書く**。

- 関数：50行
- ファイル：400行
- 1回の変更（PR相当）：500行

**設定ファイル・定数ファイル・変更点マップは行数上限の対象外。**
D-19（1ファイルへの集約）と衝突するため、長いことを理由に分割しない。

【判定】上限超過に理由コメントがあるか。

## D-11 名前で分かるようにする

コメントを足す前に名前を直す。`data` `tmp` `flag` `result2` のような名前は使わない。

【判定】レビュー時に、関数名だけ見て役割を説明できるか3つ抜き取って確認。

## D-12 動くコードを壊さない

既存の利用者に影響する変更は「禁止」ではなく**手順を踏めば可**。
旧仕様を残す → 新仕様を追加 → 移行 → 告知 → 旧仕様を削除、の順。移行期間は最低1ヶ月。

【判定】破壊的変更に移行期間の記載があるか。

## D-13 ドキュメントの更新

L3の変更では README と設計書を同時に更新する。**コードとドキュメントが食い違っていたら、
どちらが正しいかを人間に確認してから直す。**片方に機械的に合わせない（食い違いはバグの兆候）。

【判定】L3のコミットにドキュメント差分が含まれているか。

## D-14 デプロイはAIが実行する（実行前に毎回確認を取る）

**このプロジェクトは OPS_RULES.md の O-04 を適用しない。**
2026-08-19 に大野の指示で恒久的に変更した。記録は docs/exceptions.md。

AIが実行してよいのは次の3つ。

| できること | コマンド |
| :--- | :--- |
| GitHubへのpush | `git push origin main` |
| Vercelへのデプロイ | `vercel --prod`（GitHub連携で自動デプロイされる場合はpushで足りる） |
| 本番DBのマイグレーション適用 | `prisma migrate deploy` |

### 実行前に必ず提示する3点（O-03）

確認なしに実行しない。「デプロイして」と言われても、まず次を出してから実行する。

1. **何を変えるか** — このデプロイに含まれるコミットと、利用者から見て何が変わるか
2. **問題が出たらどう気付くか** — 見る画面・ログ・指標を具体的に
3. **どうやって元に戻すか** — 手順と所要時間。DBスキーマを変える場合は戻すSQLも

### それでもAIが実行しないこと

- **秘密情報の設定**（Vercelの環境変数、APIキー、`NEXTAUTH_SECRET` の生成と登録）。
  S-01。AIに値を渡さない。人間がダッシュボードで設定する。
- **リポジトリの可視性の変更**（public ⇄ private）。
- **DBの破壊的操作**（`prisma migrate reset` / テーブル削除 / WHERE句なしのUPDATE・DELETE）。D-15。
- **戻せないマイグレーション**（カラム削除・型変更など、既存データが失われるもの）。
  この種の変更は事前に大野へ影響範囲を示し、個別に承認を得る。

### 実行後に必ず書くこと

docs/releases.md の表に、**日付・何を変えたか・実行者**を記入する。
実行者欄には「Claude（大野の指示）」と書く。空欄のまま放置しない。

## D-15 禁止コマンド（AIは実行しない）

```
rm -rf                      （※1 の領域を除く）
git push --force / -f
git reset --hard            （未コミットの他人の変更を消す可能性）
git clean -fdx
npm run db:reset            （本番・共有環境向け）
prisma migrate reset
DROP / TRUNCATE
DELETE（WHERE句なし）
UPDATE（WHERE句なし）
スプレッドシートの getDataRange().setValues()（全面上書き）
本番の認証情報を使うあらゆるコマンド
```

※1 `rm -rf` を認めるのは、**そのセッション用の一時ディレクトリ配下のみ**。
プロジェクトディレクトリ・ホーム直下・デスクトップ・`~/gdrive` は対象外（「作業用に作ったつもりだった」は認めない）。

実行が必要なら、コマンドを提示して人間に依頼する。

【判定】セッションログに上記が無いか。

## D-16 セキュリティと運用は別紙が優先

`SECURITY_RULES.md` と `OPS_RULES.md` の規定は、本ファイルおよび作業効率より常に優先する。
特に **AIに渡してよいデータの線引き（S-03）** は例外なく守る。

## D-17 一括更新・一括削除は「数えてから」実行する

対象が2件以上になる更新・削除は、実行前に必ず次の3つを行う。

1. **数える** — 対象件数を先に出して表示する（ドライラン）
2. **控えを取る** — 対象データを別シート／別ファイルへ日時つきでコピーする
3. **確認する** — 想定件数と一致することを人間が見てから実行する

想定件数を宣言せずに実行する一括処理は書かない。
全件が対象になる更新（WHERE句なし／範囲が全行）は、意図的であってもL3。

**スプレッドシートへの書き込み**では、あわせて次を守る。
- `setValues` の前に、書き込む配列の行数・列数と `getRange` の範囲が一致することをコードで検証し、
  合わなければ例外を投げて中止する（1行ずれた範囲は例外にならず、静かに上書きする）
- ヘッダー行の位置や列番号を決め打ちしない。ヘッダー名から列番号を引く
- `getDataRange().setValues()` による全面上書きを使わない（D-15）

【判定】一括処理の実装に、件数カウント・控え・確認の3つが揃っているか。
　　　　`setValues` の呼び出しを全件挙げ、範囲の検証があるか確認する。

## D-18 途中で落ちても壊れないようにする

繰り返し処理・外部連携・一括書き込みには次を必ず入れる。

- **進捗を記録する** — どこまで処理したかを毎回保存し、再実行は続きから行う。
  または全件やり直しても結果が同じ（冪等）になるようにする
- **二重実行を防ぐ** — 処理IDまたは実行中フラグを持つ。同じ入力で再実行しても
  通知・請求・行追加が重複しないこと
- **時間制限に備える** — GASは6分（時間主導トリガーは30分）で強制終了する。
  1回で終わらない量は分割し、途中で終了しても中途半端なデータが残らない単位で書き込む
- **同時編集に備える** — 人間とプログラムが同じシートを触るシステムは、
  実行中フラグを立てるか、実行時間帯を分ける

【判定】外部連携・一括処理を1つ選び、**途中で止めてから再実行**し、
　　　　重複・欠落・二重通知が起きないことを確認する。

## D-19 業務ルールは1ファイルに集める

将来変わる業務上の決めごと（料金・手数料・税率・締め日・日数・しきい値・
ステータス名・区分名・権限区分・通知の文面・宛先）は、**1つの設定ファイルへ集約する。**

- 同じ決めごとをコードとスプレッドシートの数式の両方に書かない。
  どちらか一方を正とし、もう一方はそこから読む
- フォームの選択肢・入力規則・条件付き書式に区分名を直接持たせない。設定ファイルから生成する
  （手で編集した選択肢は、必ずいつかコードとずれる）
- 同じ業務ルールを2箇所以上で実装しない（D-09）
- PDF・メールのテンプレートに金額や条件を直接書かない。差し込みにする

【判定】代表的な変更を3つ選び（例：「基本料金を変える」「ステータスを1つ増やす」「通知先を変える」）、
　　　　**それぞれ修正が必要なファイル数を実際に数える。1つでも2ファイル以上になったら不合格**
　　　　（テストコードは数えない）。

## D-20 同じ業務処理への入口を複数作らない

同じ処理を呼ぶ経路（画面のボタン、メニュー、定期実行、API）は複数あってよいが、
**実際に処理する関数は1つにする。**経路ごとに処理を書き分けない。
バグを1箇所直せば全経路が直る状態を保つ。

【判定】主要な業務処理を3つ選び、呼び出し元をすべて辿って同じ関数へ集約されているか確認する。

---

## 完了の定義

以下を全て満たしたときだけ「完了」と報告してよい。

- [ ] 機械ゲートを実行し、出力を貼った（D-05）
- [ ] 秘密情報がリポジトリに入っていない（S-01）
- [ ] 変更レベルに応じた手続きを踏んだ（D-01）
- [ ] 元に戻す方法を書いた
- [ ] 直した箇所が1ファイルに収まっている（D-19）／収まらない理由を書いた
- [ ] 未確認・未対応で残した点を全て列挙した

**未完了の項目があるのに「完了しました」と報告することを、本ルールで最も重い違反とする。**
