# 変更点マップ

| 変えたいこと | 直すファイル | 直したあとに確認する画面 |
| :--- | :--- | :--- |
| 電話番号・営業時間を変える | src/config/company.ts | トップpage・ヘッダー・物件詳細・送信メール |
| 会社名・住所を変える | src/config/company.ts | フッター・送信メール本文の署名 |
| メールの差出人アドレスを変える | src/config/company.ts（MAIL_FROM） | 会員登録完了メール・問い合わせ控えメール |
| ローン金利・借入年数・想定リノベ費用を変える | src/config/loan.ts | 物件詳細の月々返済額・資金計画ページ |
| 権限区分を増やす／変える | src/config/security.ts（ROLE） | 管理画面全体・各サーバーアクション |
| ログイン試行回数・セッション有効期限を変える | src/config/security.ts | ログイン画面 |
| ログイン後の行き先を変える | src/lib/authPaths.ts（URLの組み立て）／src/app/after-login/page.tsx（権限ごとの振り分け） | ログイン直後の着地ページ |
| 物件の公開レベルの意味を変える | src/config/security.ts（DISCLOSURE_LEVEL） | 物件一覧・物件詳細 |
| メール送信を止める | Supabase の SystemSetting テーブル（key=MAIL_SENDING_ENABLED を false） | 会員登録・問い合わせ送信 |
| 管理者への通知先を変える | 環境変数 MAIL_ADMIN_ADDRESS | 問い合わせ送信後・会員登録後／/admin/mail-logs |
| メールの文面を変える | src/lib/mailPayload.ts | 会員登録完了メール・問い合わせ控え・管理者宛通知（※変更後は必ず pnpm test を実行） |
| メールが届いたか確認する | （コードではなく）管理画面 /admin/mail-logs | 送信結果の一覧 |
| 会員一覧に表示する項目を増やす | src/app/(admin)/admin/users/page.tsx | 管理画面の会員管理 |
| ローン計算式そのものを変える | src/lib/loan.ts | 物件詳細・物件カード・/simulation（※変更後は必ず pnpm test を実行） |
| トップの新着物件の件数を変える | src/app/(home)/NewArrivals.tsx（COUNT） | トップページの新着物件 |
| 物件カードの表示項目を変える | src/components/PropertyCard.tsx だけ | トップの新着・物件一覧 |
| 掲載対象のエリアを増やす／減らす | src/config/property.ts（AREAS）だけ | 物件一覧・CSV取込・トップの地図（姫路市以外の表）・会社概要 |
| 姫路市内の地区区分・小学校区を変える | src/config/himejiAreas.ts だけ | トップの地図・小学校区タブ・物件一覧の絞り込み |
| 検索条件を増やす／条件の選択肢を変える | src/lib/propertySearch.ts（検証と選択肢）**と** src/components/PropertySearchPanel.tsx（画面） | トップの検索パネル・物件一覧 |
| 間取りの区分を変える | src/lib/madori.ts（MADORI_BUCKETS） | トップの間取りタブ・検索パネル（※変更後は必ず pnpm test を実行） |
| グローバルメニュー・フッターのリンクを変える | src/config/navigation.ts だけ | 全公開ページのヘッダー・フッター |
| スタッフ紹介・代表メッセージ・選ばれる理由を変える | src/config/staff.ts だけ | トップ・会社概要 |
| 宅建業免許番号などの許認可表示を変える | src/config/company.ts（LICENSES） | フッター・会社概要 |
| アクセス解析・広告タグを入れる／変える | 環境変数（NEXT_PUBLIC_GA4_ID 等）。定義は src/config/analytics.ts | 全公開ページ（未設定ならタグは1本も出ない） |
| 物件種別を増やす／変える | src/config/property.ts（PROPERTY_TYPE） | CSV取込・物件一覧 |
| CSVの読み取り方を変える（文字コード・区切り） | src/lib/csv.ts | 管理画面の一括入力（※変更後は必ず pnpm test を実行） |
| 取込CSVの列を増やす | prisma/schema.prisma → src/app/actions/properties.ts（IncomingProperty と行組み立て）→ docs/物件CSVの形式.md | 管理画面の一括入力・物件詳細 |
| 日付の書式を増やす | src/lib/dates.ts | CSV取込の情報公開日・次回更新予定日（※変更後は必ず pnpm test を実行） |
| 管理画面の物件編集フォームに項目を足す／並びを変える | src/config/propertyFields.ts | /admin/properties/[id] の編集フォーム（保存処理も同じ定義を読む） |
| 画像の枚数・サイズ・形式の上限を変える | src/config/images.ts | 管理画面の画像アップロード（一括・個別とも） |
| 画像のファイル名の規則を変える | src/lib/imageName.ts | 一括アップロードの紐づけ（※変更後は必ず pnpm test を実行） |
| 画像の保管先を変える | src/lib/storage.ts | 画像のアップロード・削除・表示 |
| 管理画面の文字サイズ・余白・角丸をまとめて変える | src/app/(admin)/admin.css（Tailwind のトークンを px で上書きしている） | 管理画面の全ページ |
| 管理画面の左メニューの項目を増やす／減らす | src/app/(admin)/AdminNav.tsx | 管理画面のサイドバー |
| ダッシュボードの数値・「対応が必要なこと」の条件を変える | src/app/actions/dashboard.ts | /admin のダッシュボード |
| 会員データとして画面へ返す列を増やす | src/app/actions/users.ts（USER_FIELDS）**のみ**。ここに書いた列だけがブラウザへ送られる | 会員管理・会員詳細 |


**管理画面の寸法は `src/app/(admin)/admin.css` に集約している。**
公開サイトの `globals.css` は `html { font-size: 62.5% }`（1.6rem = 16px）を前提に書かれており、
Tailwind の rem 由来の寸法がそのままでは 62.5% に縮む。管理画面の根要素 `.admin-root` に
px のトークンを載せて打ち消しているため、管理画面の見た目が全体的におかしいときは
まずこのファイルを見ること。

**エリアの行は3ファイルにまたがっている（D-19 違反）。**地図のSVGが確定版HTMLの中にあり、
クリック処理が main.js にあるため、いまは1箇所に寄せられていない。エリアを変えるときは
必ず3箇所とも直すこと。docs/debt.md に起票済み。

このマップに2ファイル以上書かれている行は D-19 違反の候補として扱う。マップは散らばりを許すための地図ではなく、散らばりを見つけるための道具である。
