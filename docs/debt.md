# 技術的負債

将来的に修正が必要な箇所や改善項目を記録します。

| 日付 | 場所 | 何が問題 | 放置したときのリスク | 想定工数 |
| :--- | :--- | :--- | :--- | :--- |
| ~~2026-08-18~~ | ~~リポジトリ直下の使い捨てスクリプト群~~ | **解決済み（2026-08-18）** 大野の承認を得て全て削除（verify-users.js / test-user-db.js / check-lib.js / update_layout.js / generate_member.js / prisma/seed.js） | — | 完了 |
| 2026-08-18 | index.html（リポジトリ直下） | Next.js 移行前の静的HTML 1027行。会社情報が直書きでconfig集約の対象外。temp.html / temp_member.html は削除済み | 電話番号などを変更したときに直し漏れる（D-19）。ただし配信されないファイルなので実害は限定的 | 0.5h（削除するだけなら即） |
| 2026-08-18 | src/app/actions/registerUser.ts | 退会済み（deletedAt あり）のメールアドレスでは再登録できず、窓口案内のみになる。email が一意制約のため | 退会した人が再入会できず、問い合わせ対応の手間が増える | 2h（復帰フローの設計含む） |
| 2026-08-18 | src/app/(home)/page.tsx | 848行。dangerouslySetInnerHTML に確定版HTMLを丸ごと埋め込んでおり、部分的な修正がしにくい | 文言修正のたびに巨大な差分が出て、レビューで変更点を見落とす | 4h以上（分割にはデザイン検証が必要） |
| 2026-08-18 | src/app/(admin)/admin/page.tsx（ダッシュボード） | 登録会員数1,284・掲載物件数45などの統計値がハードコードされたダミー。最近の登録会員も固定の架空データ | 管理者が実データと誤認して経営判断する | 2h |
| 2026-08-19 | package.json / nodemailer | Resend へ移行したため `nodemailer` と `@types/nodemailer` はどこからも import されていない。D-04（勝手に消さない）に従い残置している | 使っていない依存が `pnpm audit` の監視対象として残り、脆弱性が出るたびに判断を迫られる | 0.2h（削除の承認が取れれば即） |
| 2026-08-19 | Vercel の契約プラン | Hobby プランは規約上**非商用利用に限る**が、本件は商用サイト。private リポジトリではデプロイ元の制限も受ける | 規約違反の状態で本番公開することになる。また git 連携の自動デプロイが使えず、デプロイ手順が属人化する | $20/月（Proへ変更するだけ） |
| 2026-08-19 | src/app/(home)/page.tsx の新着物件カード3枚 | 表示している物件情報が固定のダミー（DBの物件は0件）。リンク先は /properties に直したが、カードの中身は架空のまま | 来訪者が実在しない物件を見て問い合わせる。管理画面ダッシュボードの偽統計と同じ問題 | 3h（DBから取得する実装） |
| 2026-08-19 | パスワード再設定の導線 | 機能が存在しない。トップの「ID・パスワードをお忘れの方はこちら」は href="#" の死にリンクだったため、ログインフォームの置換に伴い削除した | パスワードを忘れた会員が自力で復帰できず、窓口対応になる | 6h（トークン発行とメール送信の設計含む） |
| 2026-08-19 | フッターの「サイトマップ」リンク | 該当ページが無く href="#" のまま。今回は据え置いた | 押しても何も起きず、壊れている印象を与える | 0.2h（削除するだけなら即） |
| 2026-08-19 | 管理画面の物件編集（updateProperty） | 保存時に変更前の値を残していない。押した時点で即上書きになる。CSV取込には控え（PropertyImportBackup）があるが、画面編集には無い | 誤操作で物件情報を上書きしたとき復旧できない。件数が増えるほど影響が大きい | 3h（変更履歴テーブルの新設） |
| ~~2026-08-19~~ | ~~取扱店を画面に出していない~~ | **解決済み（2026-08-19）** 大野の指示により公開側の物件詳細に表示した | — | 完了 |
| 2026-08-19 | src/config/company.ts | **自社（大井建設工業）の宅地建物取引業の免許番号が無い。**いま画面に出しているのは athome の元付業者の免許番号であって、広告主である自社のものではない | 広告主の表示義務を満たしていない可能性。取引態様も元付業者のものを出しており、誰が広告主か不明瞭 | 0.5h（番号を教えてもらえれば即） |
| 2026-08-19 | 掲載対象エリアの定義が3箇所 | src/config/property.ts の AREAS、public/assets/js/main.js の CODE2CITY、トップの地図SVGの is-target。今回1つずつ手で揃えたが、次に変えるときも3箇所を直す必要がある | エリアを追加したのに地図から押せない、逆に対象外が押せる、といった食い違いが再発する（実際に立科町が押せず上田市が押せる状態だった） | 4h（地図SVGのReact化が必要） |
