# インシデント記録

不具合や障害の発生内容と対応を記録します。

| 発生日時 | 発見の経緯 | 影響範囲(いつから・どのデータ・何件) | 初動でやったこと | 恒久対策 | 顧客への報告日時 |
| :--- | :--- | :--- | :--- | :--- | :--- |
| 2026-08-18 | Supabase接続設定の作業中、DBパスワードがチャットに貼られた | 新規作成直後の空DB。テーブル0件・顧客データ0件。影響なし | 直ちにローテーション（Supabase → Reset database password）。旧パスワードは失効済み | 接続情報は `.env.migrate`（gitignore済）に書き、画面表示は伏せ字にする方式へ変更。`migrate-supabase.sh` として手順化 | 不要（顧客データの関与なし） |
| | | | | | |
| 2026-08-19 11:31 | 物件CSVの2回目の取込が「取込に失敗しました（お問い合わせID: 202608190231-84UBO）」で失敗 | 本番。物件3件は既に取込済みで、追加・更新は0件。**トランザクションにより巻き戻り、データ破損なし** | エラーIDから importProperties の catch を特定し、控えを取る処理の JSON.stringify が BigInt で落ちていることを確認 | objMngNo を BigInt にした際の作り込み。src/lib/json.ts の toJsonSafe を通すよう修正し、テスト6件を追加。既存0件のうちは通り2回目で初めて落ちる壊れ方だったため、空配列のケースもテストに含めた | 不要（社内検証中・顧客影響なし） |
| 2026-08-19 | コミットメッセージの記載誤り（作業記録の不正確さ） | eb104a7 に「Zoho SalesIQ を削除」と書いたが、実際の削除は 88dc11d に紛れ込んでおり、そのコミットメッセージには記載が無かった。git add -A でまとめてステージしたことによる | コード・本番への影響なし。記録のみの問題 | 変更の記録が実態とずれると、後から「いつ何を変えたか」を追えなくなる。関連の薄い変更を同じコミットに混ぜないこと。git add -A の前に git status を確認する | 不要 |
| 2026-08-19 14時台 | 大野から「佐久市をクリックしても物件が表示されない」と報告 | 本番のトップページ。地図・資金計画シミュレーター・クッキーバーが全て無反応（main.js の機能すべて）。表示と物件データには影響なし | ブラウザで検証。main.js を手動で eval すると全機能が正常に動いたため、スクリプトの中身ではなく読み込みタイミングの問題と特定 | 92616c3 でHTMLブロブを2つに分割したことが引き金。<Script strategy="lazyOnload"> をやめ、useEffect からDOM構築後に読み込む形に変更した。main.js は先頭で要素を探し、無ければ何もせず終わる作りのため、タイミングを外すと全機能が黙って停止する | 不要（社内検証中） |
| 2026-08-31 13時台 | 大野から「管理画面にログインできない」と報告 | 本番。2026-08-25 の構築時から一貫してログイン不能。ログイン画面に戻るだけで、エラーも出ない。データ破損なし（DBへの書き込みが一度も発生していない） | 本番DBを読み取りのみで確認。User は1件（`oono@awn.jp`／ADMIN／ACTIVE／ロックなし／`$2b$` 60文字のハッシュあり）、RateLimit は検証で作った1件のみ、LOGIN ログは構築日の2件のみ。**失敗の痕跡すら無い＝`authorize()` が呼ばれていない**と判断し、認証コードではなく経路を疑った | Vercel の `NEXTAUTH_URL` が Deployment Protection 配下の別名 `…-o-dec4.vercel.app` を指していた。NextAuth の組み込みログイン画面はフォームの action を `NEXTAUTH_URL` から組み立てるため、公開URLで開いた画面が保護側へ POST し、Vercel の SSO に横取りされていた。`NEXTAUTH_URL` と `NEXT_PUBLIC_SITE_ORIGIN` を公開URL `https://morishita-used-housing-site.vercel.app` へ揃え、本番へ再デプロイ | 不要（社内検証中・顧客影響なし） |
