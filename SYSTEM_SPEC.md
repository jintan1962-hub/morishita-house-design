# 中古住宅×リノベーション RENOEL システム仕様書

## 1. プロジェクト概要
中古住宅の検索、会員限定物件の閲覧、およびリノベーションを前提とした資金計画シミュレーションを提供する、不動産×リノベーション特化型のWebシステム。

## 2. 技術スタック
- **Frontend**: Next.js 16 (App Router), TypeScript, TailwindCSS
- **Backend**: Next.js Server Actions, API Routes
- **Database**: PostgreSQL (Supabase)
- **ORM**: Prisma
- **Authentication**: NextAuth.js (Credentials Provider)
- **Email**: Resend (REST API を fetch で直接呼び出し。SDKは使わない)
- **Icons**: Lucide React
- **Design**: 独自CSS (style.css) によるリニューアルデザインを適用

## 3. システム構成
### 3.1 ページ構成
- **フロントエンド (/(home), /(public))**
    - トップページ (`/`): リニューアル版デザイン
    - 会員登録 (`/member`): 4ステップ（入力・希望条件・確認・完了）のマルチステップフォーム
    - 物件一覧 (`/properties`): 公開/非公開物件の出し分け機能
    - 物件詳細 (`/property/[id]`): 物件情報表示、ローンシミュレーション
    - 物件問い合わせ (`/property/[id]/contact`): 物件専用フォーム
    - マイページ (`/mypage`): 会員情報表示、お気に入り(予定)
    - プロフィール編集 (`/mypage/edit`): 名前・電話番号の変更
- **管理画面 (/(admin)/admin)**
    - ダッシュボード: 統計概要
    - 会員管理 (`/admin/users`): 会員一覧、詳細表示、退会処理
    - 物件管理 (`/admin/properties`): 物件情報の管理
    - お問い合わせ管理 (`/admin/inquiries`): 問い合わせ一覧、詳細確認、対応ステータス管理

### 3.2 データベース（Prisma Schema）
- **User**: 会員情報（名前、メール、パスワード、電話番号、会員種別など）
- **Property**: 物件情報（価格、エリア、公開レベル、間取りなど）
- **ActivityLog**: 行動履歴（ログイン、物件閲覧、問い合わせなどを自動記録）
- **Inquiry**: 物件へのお問い合わせ（お名前、内容、対応ステータス、完了時刻など）
- **SystemSetting**: 運用スイッチ（key/value）。`MAIL_SENDING_ENABLED` でメール送信を止められる
- **PropertyImportBackup**: 物件一括取込の直前の既存データ（上書き前の控え・戻す用）

## 4. 主要機能仕様
### 4.1 会員管理
- **登録フロー**: 入力内容のリアルタイム確認、パスワードのハッシュ化（bcryptjs）、登録完了時の自動メール送信。
- **ログイン**: メールアドレスとパスワードによる認証。セッションによる状態保持。
- **行動追跡**: 会員が「いつログインしたか」「どの物件を閲覧したか」を自動的に `ActivityLog` へ記録。
- **退会**: ユーザー自身の操作による**論理削除**（`deletedAt` に日時、`deletedBy` に 'SELF' を記録）。
  レコードは残るため誤操作から復旧できる。参照系のクエリは `deletedAt: null` で絞る。
  ※退会済みメールアドレスでの再登録は現状できない（docs/debt.md 起票済み）。

### 4.2 物件閲覧制限
- **公開レベル 0 (一般公開)**: 全ユーザーが閲覧可能。
- **公開レベル 1 (会員限定)**: 未ログインユーザーには、**サーバー側で価格・所在地・間取り・画像を含まないデータを返す**。
  画面で隠すのではなく、そもそも渡さない（ページソースからも読めない）。ログイン後はすべての情報を開示。

### 4.3 お問い合わせ
- **連携**: 物件詳細からのお問い合わせ時、DBへ保存すると同時に、ユーザーと管理者へ自動メール送信。
- **管理**: 管理画面で「対応済」にチェックを入れると、対応完了時刻が自動記録される。

### 4.4 メール送信
- **送信基盤**: Resend。`src/lib/mail.ts` が REST API（`https://api.resend.com/emails`）を fetch で呼ぶ。
  新規の依存パッケージは追加していない。本文の組み立ては `src/lib/mailPayload.ts`（テストあり）。
- **自動返信**: 会員登録完了時とお問い合わせ完了時に、本人宛へ即時送信。
- **管理者通知**: 同じタイミングで `MAIL_ADMIN_ADDRESS` 宛にも通知を送る。返信先（`reply_to`）は
  お客様のアドレスにしてあるので、受信箱からそのまま返信できる。宛先が未設定なら送らず、記録だけ残す。
- **送信結果の記録**: 全ての送信は `MailLog` に SENT / FAILED / SKIPPED として残り、
  管理画面 `/admin/mail-logs` から確認できる。
  以前は送信に失敗しても `console.error` に出るだけで、運用側からは気付けなかった。
- **メールが送れなくても受付は成立させる**: 会員登録・お問い合わせの保存が終わったあとに送信するため、
  送信の失敗が登録・受付を巻き戻すことはない。
- **前提**: 実際に届かせるには Resend 側で送信ドメインの認証（SPF / DKIM の DNS 登録）が必要。
  未認証の状態では `onboarding@resend.dev` から Resend アカウントの登録アドレス宛にしか送れない。

## 5. 運用・保守
- **インフラ**: Vercel (Frontend/Backend), Supabase (Database)
- **環境変数**: `.env` ファイルによる認証情報・接続文字列の管理
- **セキュリティ**（2026-08-18の検品と是正を反映。以前の記述は実装と食い違っていた）:
    - パスワードは `bcrypt` によりハッシュ化して保存。
    - **全サーバーアクションの冒頭で認可を判定する**（`src/lib/auth.ts` の `requireAdmin` / `requireUser`）。
      `"use server"` の関数は画面を経由せず直接POSTできるため、画面側の制御だけでは防げない。
    - 権限は毎回DBから読み直す（`role` / `status` / `deletedAt`）。セッションの値を信用しない。
    - ログイン試行は5回でアカウントを15分ロック。セッション有効期限は8時間（`src/config/security.ts`）。
    - 画面には内部情報（スタックトレース・SQL・パス）を出さず、問い合わせ用IDのみ返す（`src/lib/errors.ts`）。
    - `NEXTAUTH_SECRET` は未設定なら起動を失敗させる。フォールバック値は持たない。

- **運用の安全装置**:
    - メール送信の停止スイッチ：`SystemSetting` テーブルの `MAIL_SENDING_ENABLED` を `false`（デプロイ不要・即時）
    - 物件の一括取込：件数確認・上書き前データの控え（`PropertyImportBackup`）・トランザクションを必須化
    - 開発ルールは `CLAUDE.md` と `~/Desktop/システム開発マスタールール/v2/` に従う
